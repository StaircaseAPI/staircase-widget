/// <reference types="react" />
interface Props {
    multiple?: boolean;
    types: string[];
    handleChange: (ev: any) => any;
}
export declare const FileFormControlComponent: ({ multiple, types, handleChange }: Props) => JSX.Element;
export {};
